apiVersion: v1
kind: Service
metadata:
  labels:
    name: pttg-ip-fm-ui
  name: pttg-ip-fm-ui
spec:
  type: NodePort
  ports:
  - name: https
    port: 443
  - name: http
    port: 80
    targetPort: https
    nodePort: ${pttg_ip_fm_ui_port}
  selector:
    name: pttg-ip-fm-ui
